# application name should be "ticket-service" and the application configuration should
# be retrieved from "http://localhost:8888 which is the URL for externalized ConfigServer"
spring:
  application:
    name: ticket-service
  cloud:
    config:
      uri: http://localhost:8888
